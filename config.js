// Fill these in after you've created your GitHub repo (see README.md).
// Your access token is NOT stored here — it's entered once inside the
// app itself and kept only in your phone's local storage, so it never
// ends up committed to this (public) repo. See README.md for why.
window.APP_CONFIG = {
  // e.g. "myusername"
  githubOwner: "",
  // e.g. "timetable-app"
  githubRepo: "",
  // Public VAPID key (safe to expose — this one is already generated for you):
  vapidPublicKey: "BBI68GAkByHP3Q8e7g2F-XJYkPAWAUJYMo5czXPKAvWljhy_28nxed5b19W1YvSGE_tKbnCCixdRQETfOnvj73Q",
};
